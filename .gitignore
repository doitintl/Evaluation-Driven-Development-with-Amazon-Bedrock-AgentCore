node_modules
**/.DS_Store

# Build/audit scratch (yudho's pivot work)
.workshop-build/

# Environment files with credentials
.env
**/.env

# Solution files - attendees build these themselves
# solutions/

**/__pycache__

# Workshop Studio build artifacts (regenerated by package_for_workshop.sh)
workshop-scaffold/workshop/assets/

# Archived pre-refactor content (kept locally for reference, not source of truth)
workshop-scaffold/workshop/.archived-content/

# Python venvs (per-agent)
**/.venv/

# tsx and TS build output
**/dist/
**/.tsbuildinfo

# Strands AgentCore Runtime build artifacts
travel-agent-strands/runtime/.bedrock_agentcore/

# Holmes scan packages (regenerated locally for security review)
holmes/

# Local MCP / tooling logs
logs/
**/*.log
**/*.env.*
# Attend-all run transients (canonical copies live under solutions/module-1/)
/langfuse-adapter/
/openinference-aws-adapter/
/lambda/
/cfn
.attend-all-session
/session-*/

# Attend-all session workspaces (code + node_modules + evidence; portable but not for the repo)
sessions/

# Eval reports are participant/dev output, never shipped: the runner mkdirs results/ itself.
travel-agent/results/
