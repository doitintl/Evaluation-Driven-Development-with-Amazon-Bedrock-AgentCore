# Luminara travel-agent (Strands) — environment template
# Copy to .env and fill in. The CLI reads AWS_REGION and MODEL_ID via load_config().

AWS_REGION=us-east-1
MODEL_ID=us.anthropic.claude-sonnet-4-6

# Optional: pin an AWS profile if you have multiple
# AWS_PROFILE=ml-sandbox
